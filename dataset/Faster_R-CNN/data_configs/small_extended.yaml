# Images and labels direcotry should be relative to train.py
TRAIN_DIR_IMAGES: './data/small_extended/train'
TRAIN_DIR_LABELS: './data/small_extended/train'
VALID_DIR_IMAGES: './data/small_extended/valid'
VALID_DIR_LABELS: './data/small_extended/valid'
# Class names.
CLASSES: [
    '__background__', 'submarine', 'Portanaves', 'Fragata', 'cruises', 'tugboat', 'helicopter'
    ]
# Number of classes (object classes + 1 for background class in Faster RCNN).
NC: 7
# Whether to save the predictions of the validation set while training.
SAVE_VALID_PREDICTION_IMAGES: True